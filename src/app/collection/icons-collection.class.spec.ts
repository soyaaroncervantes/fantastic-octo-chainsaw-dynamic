import { IconsCollection.Class } from './icons-collection.class';

describe('IconsCollection.Class', () => {
  it('should create an instance', () => {
    expect(new IconsCollection.Class()).toBeTruthy();
  });
});
