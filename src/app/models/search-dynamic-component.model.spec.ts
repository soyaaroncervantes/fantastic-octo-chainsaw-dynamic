import { SearchDynamicComponent.Model } from './search-dynamic-component.model';

describe('SearchDynamicComponent.Model', () => {
  it('should create an instance', () => {
    expect(new SearchDynamicComponent.Model()).toBeTruthy();
  });
});
