import { CreateComponentDirective } from './create-component.directive';

describe('CreateComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateComponentDirective();
    expect(directive).toBeTruthy();
  });
});
